#include<bits/stdc++.h>
using namespace std;
#define pb push_back
#define st first
#define nd second
#define db(x) cerr << #x << " = " << x << endl
#define _ << ", " << 
typedef long long ll;
const int N = 5e3 + 5;
int a[N];
vector<vector<int>> ns[N];

int main(){
  int n, k, v;
  cin >> n >> k >> v;

  for(int i = 0; i < n; i++)
    cin >> a[i];

  ns[0].push_back(vector<int>());
  for(int i = 0; i < n; i++)
    for(int j = 0; j < k; j++)
      ns[j + ];
    
}
